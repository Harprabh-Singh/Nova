/* ============================================================================
   Closing rule for the evidence wall. Technical signature on one side, the
   chapter stamp on the other. Nothing else — the wall has already spoken.
   ========================================================================== */

export function NumbersFooter() {
	return (
		<footer className="nbr-foot">
			<span className="nbr-foot-sig">Measured / Traceable / Versioned / Local-first</span>
			<span className="nbr-foot-stamp">NOVA // By the numbers</span>
		</footer>
	)
}

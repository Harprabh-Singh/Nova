/* ============================================================================
   Bottom rule. Technical signature on one side, brand stamp on the other —
   the same page marker used across the NOVA environment.
   ========================================================================== */

export function AccessFooter() {
	return (
		<footer className="access-footer">
			<span>Retrieval-first // Secure // Multi-tenant // Grounded</span>
			<span>NOVA // Enterprise Knowledge Intelligence</span>
		</footer>
	)
}
